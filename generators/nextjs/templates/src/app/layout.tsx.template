import type { Metadata } from 'next'

export const metadata: Metadata = {
  title: '{{APP_NAME}}',
  description: 'Built with ⛵ Ferry v{{FERRY_VERSION}}',
}

export default function RootLayout({
  children,
}: {
  children: React.ReactNode
}) {
  return (
    <html lang="en">
      <head>
        <link rel="stylesheet" href="/style.css" />
      </head>
      <body>{children}</body>
    </html>
  )
}
