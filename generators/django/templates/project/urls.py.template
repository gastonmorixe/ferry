from django.urls import path
from . import views

urlpatterns = [
    path("",        views.index,      name="index"),
    path("json",    views.json_view,  name="json"),
    path("xml",     views.xml_view,   name="xml"),
    path("text",    views.text_view,  name="text"),
    path("health",  views.health,     name="health"),
]
